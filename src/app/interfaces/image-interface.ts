// Simple interface to hold information such as - 

// Src: the source of the file, etc. base64 string
// Name: the name of the file

export interface ImageInterface {
    
    src: string;
    name: string;
}
